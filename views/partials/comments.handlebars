<div class="comments">
  {{#each this}}
  <section class="comment">
    <div class="user-details">
      {{user.username}} on {{created_at}}
    </div>
    <div class="content">
      {{comment_content}}
    </div>
  </section>
  {{/each}}
</div>