<div class="comments">
  {{#each comments as |comment|}}
    <div class="comment">
      <p>{{comment.comment_content}}</p>
      <p>Posted by {{comment.username}} on {{format_date comment.created_at}}</p>
    </div>
  {{!-- {{/each}}
  <section class="comment">
    <div class="user-details">
     Posted by {{username}} on {{created_at}}
    </div>
    <div class="content">
      {{comment_content}}
    </div>
  </section> --}}
  {{/each}}
</div>