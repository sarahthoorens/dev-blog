<div class="comments">
  {{#each this}}
  <section class="comment">
    <div class="">
      {{user.username}} {{date}}
    </div>
    <div class="text">
      {{comment_content}}
    </div>
  </section>
  {{/each}}
</div>