<div="container-fluid">
   <div class="card text">
      <div class="card-header">
         {{title}}
      </div>
      <div class="card-body">
         {{!-- <h5 class="card-title">Special title treatment</h5> --}}
         <p class="card-text">{{content}}</p>
         <a href="/entry/:title" class="btn btn-primary">Go to post</a>
      </div>
      <div class="card-footer text-muted">
         {{ author }} {{ entry_date}}
      </div>
   </div>
   </div>